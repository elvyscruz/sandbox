// Tuples indicates the order of values

type Drink = [string, boolean, number]

const pepsi: Drink = ['pepsi',true,40]

// we get an error
// pepsi[0] = 40

const sprite: Drink = ['clear', true, 30]
const tea: Drink = ['brown', false, 0]